import { loadRuntimeConfig, hasSupabaseConfig } from "./config.js";
import { createSupabaseClient } from "./db/supabaseClient.js";
import { financialEngine } from "./financial-engine.js";

const STYLE_ID="borjai-2-style",PANEL_ID="borjai-2-panel",NAV_ID="borjai-2-nav";
const euro=new Intl.NumberFormat("es-ES",{style:"currency",currency:"EUR",maximumFractionDigits:0});
const pct=new Intl.NumberFormat("es-ES",{style:"percent",maximumFractionDigits:1});
const money=v=>euro.format(Number(v||0)),percent=v=>pct.format(Number(v||0));
const month=(d=new Date())=>d.toISOString().slice(0,7);
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
function injectStyle(){if(document.getElementById(STYLE_ID))return;const s=document.createElement("style");s.id=STYLE_ID;s.textContent=`.b2-panel{position:fixed;inset:0;z-index:120;background:#090a0d;color:#f5f7fa;overflow:auto;padding:28px 28px 50px;font-family:inherit}.b2-wrap{max-width:1420px;margin:0 auto}.b2-top{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:24px}.b2-kicker{font-size:11px;letter-spacing:.16em;text-transform:uppercase;color:#8f96a3;font-weight:800}.b2-top h1{margin:5px 0 6px;font-size:32px;letter-spacing:-.04em}.b2-top p{margin:0;color:#8f96a3}.b2-close{border:1px solid #2c3038;background:#15181d;color:#dfe3e8;border-radius:10px;padding:10px 14px;cursor:pointer}.b2-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:14px;margin-bottom:18px}.b2-card,.b2-section{background:#111419;border:1px solid #242831;border-radius:16px;padding:18px;box-shadow:0 8px 30px rgba(0,0,0,.14)}.b2-section{border-radius:18px;margin-bottom:16px}.b2-label{font-size:12px;color:#8f96a3}.b2-value{font-size:26px;font-weight:800;margin-top:7px;letter-spacing:-.03em}.b2-sub,.b2-section>p{font-size:12px;color:#8f96a3;margin-top:6px}.b2-section>p{margin-bottom:16px}.b2-good{color:#3bd486}.b2-warn{color:#ffbf4b}.b2-bad{color:#ff5965}.b2-sections{display:grid;grid-template-columns:1.1fr .9fr;gap:16px}.b2-section h2{font-size:17px;margin:0 0 5px}.b2-list{display:flex;flex-direction:column;gap:10px}.b2-row{display:grid;grid-template-columns:1fr auto;gap:14px;align-items:center;padding:11px 0;border-bottom:1px solid #20242b}.b2-row:last-child{border-bottom:0}.b2-row strong{font-size:13px}.b2-row small{display:block;color:#7f8794;margin-top:3px}.b2-pill{display:inline-flex;align-items:center;border-radius:999px;padding:4px 8px;font-size:10px;font-weight:800;background:#1b2027;color:#aeb5c0}.b2-bar{height:7px;background:#22262e;border-radius:99px;overflow:hidden;margin-top:8px}.b2-bar i{display:block;height:100%;background:#f32d3a;border-radius:inherit}.b2-tools{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.b2-tool{background:#0d1014;border:1px solid #22262e;border-radius:14px;padding:15px}.b2-tool h3{font-size:14px;margin:0 0 6px}.b2-tool p{font-size:11px;color:#808896;line-height:1.45;margin:0 0 12px}.b2-input{width:100%;box-sizing:border-box;background:#090b0e;border:1px solid #30343d;color:#fff;border-radius:9px;padding:10px}.b2-btn{border:0;background:#f32d3a;color:#fff;border-radius:9px;padding:10px 12px;font-weight:800;cursor:pointer}.b2-result{margin-top:10px;padding:10px;border-radius:9px;background:#171b21;font-size:12px;line-height:1.45}.b2-empty{padding:22px;border:1px dashed #30343c;border-radius:12px;color:#818997;text-align:center;font-size:12px}.b2-nav{display:flex!important;align-items:center;gap:10px}.b2-nav-mark{width:20px;height:20px;border-radius:6px;background:#f32d3a;color:#fff;display:inline-flex;align-items:center;justify-content:center;font-size:10px;font-weight:900}.b2-table{width:100%;border-collapse:collapse;font-size:12px}.b2-table th{text-align:left;color:#747c89;font-size:10px;text-transform:uppercase;letter-spacing:.08em;padding:8px;border-bottom:1px solid #292e36}.b2-table td{padding:10px 8px;border-bottom:1px solid #20242b}@media(max-width:1000px){.b2-grid{grid-template-columns:repeat(2,1fr)}.b2-sections{grid-template-columns:1fr}}@media(max-width:600px){.b2-panel{padding:18px 14px 40px}.b2-top h1{font-size:26px}.b2-grid{grid-template-columns:1fr}.b2-tools{grid-template-columns:1fr}.b2-top{align-items:center}}`;document.head.appendChild(s)}
async function getData(){const config=await loadRuntimeConfig();if(!hasSupabaseConfig(config))throw new Error("No hay configuración de datos disponible.");const client=await createSupabaseClient(config),u=await client.auth.getUser();if(u.error||!u.data?.user)throw new Error("La sesión ha caducado. Vuelve a iniciar sesión.");const uid=u.data.user.id,read=async table=>{const r=await client.from(table).select("*").eq("user_id",uid).order("created_at",{ascending:true});if(r.error)throw r.error;return r.data||[]};const [transactions,accounts,assets,liabilities,goals,investments,snapshots]=await Promise.all(["transactions","accounts","assets","liabilities","goals","investments","wealth_snapshots"].map(read));return{transactions,accounts,assets,liabilities,goals,investments,snapshots}}
function categories(data,m){const map={};for(const t of data.transactions)if(["expense","fee"].includes(t.type)&&String(t.date||"").slice(0,7)===m){const k=t.category_name||"Otros";map[k]=(map[k]||0)+Math.abs(Number(t.amount||0))}return Object.entries(map).map(([name,value])=>({name,value})).sort((a,b)=>b.value-a.value)}
function render(data){const e=financialEngine,h=e.healthScore(data,month()),b=e.balances(data),tot=e.monthlyTotals(data.transactions,month()),rec=e.recurringPayments(data.transactions),an=e.anomalies(data.transactions,month()),dups=e.duplicates(data.transactions),cats=categories(data,month()),hist=e.historicalMonthlyExpenses(data.transactions,4),prev=hist[1]?.expenses||0,nw=b.netWorth,liq=b.liquid,debt=b.debt,scoreClass=h.score>=75?"b2-good":h.score>=50?"b2-warn":"b2-bad",futureRecurring=rec.filter(r=>r.next>=new Date()).reduce((s,r)=>s+r.amount,0);return `<div class="b2-wrap"><div class="b2-top"><div><div class="b2-kicker">BorjaAI · Financial Intelligence</div><h1>Centro financiero 2.0</h1><p>Diagnóstico, control y decisiones basadas en una única fuente financiera.</p></div><button class="b2-close" data-b2-close>Cerrar</button></div><div class="b2-grid"><div class="b2-card"><div class="b2-label">Salud financiera</div><div class="b2-value ${scoreClass}">${h.score}/100</div><div class="b2-sub">Ahorro ${percent(h.savingsRate)} · liquidez ${h.monthsLiquidity.toFixed(1)} meses</div></div><div class="b2-card"><div class="b2-label">Patrimonio neto</div><div class="b2-value">${money(nw)}</div><div class="b2-sub">Activos ${money(b.investedAssets)} · deuda ${money(debt)}</div></div><div class="b2-card"><div class="b2-label">Liquidez</div><div class="b2-value">${money(liq)}</div><div class="b2-sub">Próximos recurrentes estimados: ${money(futureRecurring)}</div></div><div class="b2-card"><div class="b2-label">Este mes</div><div class="b2-value">${money(tot.expenses)}</div><div class="b2-sub">Ingresos ${money(tot.income)} · neto ${money(tot.net)}</div></div></div><div class="b2-sections"><div><section class="b2-section"><h2>Lectura de BorjaAI</h2><p>Todos los indicadores financieros usan el motor compartido.</p><div class="b2-list">${h.score===0?'<div class="b2-empty">Aún no hay datos económicos suficientes.</div>':`<div class="b2-row"><div><strong>${h.savingsRate>=.2?"Buen ritmo de ahorro":"El ahorro necesita atención"}</strong><small>Tasa de ahorro mensual: ${percent(h.savingsRate)}</small></div><span class="b2-pill">${percent(h.savingsRate)}</span></div>`}<div class="b2-row"><div><strong>${h.monthsLiquidity>=3?"Reserva razonable":"Reserva por debajo del objetivo"}</strong><small>${h.monthsLiquidity.toFixed(1)} meses de gasto cubiertos.</small></div><span class="b2-pill">${h.monthsLiquidity.toFixed(1)}x</span></div><div class="b2-row"><div><strong>${debt===0?"Sin deuda registrada":"Peso de la deuda"}</strong><small>${debt===0?"No hay pasivos registrados.":"La deuda representa "+percent(h.debtRatio)+" del patrimonio neto."}</small></div><span class="b2-pill">${money(debt)}</span></div>${an.length?`<div class="b2-row"><div><strong>Gasto inusual detectado</strong><small>${esc(an[0].merchant)} · ${money(an[0].amount)} frente a ${money(an[0].average)} de media.</small></div><span class="b2-pill b2-warn">ALERTA</span></div>`:""}${dups.length?`<div class="b2-row"><div><strong>Posibles duplicados</strong><small>${dups.length} movimientos con coincidencia fuerte.</small></div><span class="b2-pill b2-warn">REVISAR</span></div>`:""}</div></section><section class="b2-section"><h2>Gasto por categoría</h2><p>Distribución real del mes actual.</p><div class="b2-list">${cats.length?cats.slice(0,8).map(c=>{const ratio=tot.expenses?c.value/tot.expenses:0;return`<div><div class="b2-row"><div><strong>${esc(c.name)}</strong><small>${money(c.value)} · ${Math.round(ratio*100)}% del gasto</small></div><span class="b2-pill">${Math.round(ratio*100)}%</span></div><div class="b2-bar"><i style="width:${Math.min(100,ratio*100)}%"></i></div></div>`}).join(""):'<div class="b2-empty">No hay gastos registrados este mes.</div>'}</div></section><section class="b2-section"><h2>Comparativa mensual</h2><p>Último mes completo frente al anterior disponible.</p><table class="b2-table"><thead><tr><th></th><th>Anterior</th><th>Actual</th><th>Variación</th></tr></thead><tbody><tr><td>Gastos</td><td>${money(prev)}</td><td>${money(tot.expenses)}</td><td class="${tot.expenses>prev?"b2-bad":"b2-good"}">${prev?`${tot.expenses>prev?"+":""}${money(tot.expenses-prev)}`:"—"}</td></tr></tbody></table></section></div><div><section class="b2-section"><h2>Herramientas</h2><p>Decisiones calculadas por el mismo motor financiero.</p><div class="b2-tools"><div class="b2-tool"><h3>¿Me lo puedo permitir?</h3><p>Considera liquidez, próximos recurrentes y colchón histórico.</p><input class="b2-input" id="b2-afford" type="number" min="0" step="10" placeholder="Importe en €"><button class="b2-btn" style="margin-top:8px" data-b2-afford>Analizar</button><div class="b2-result" id="b2-afford-result">Introduce un importe.</div></div><div class="b2-tool"><h3>Recurrentes</h3><p>Pagos mensuales detectados.</p>${rec.length?`<div class="b2-list">${rec.slice(0,6).map(r=>`<div class="b2-row"><div><strong>${esc(r.merchant)}</strong><small>${esc(r.category)} · próximo aprox. ${r.next.toLocaleDateString("es-ES")}</small></div><span class="b2-pill">${money(r.amount)}</span></div>`).join("")}</div>`:'<div class="b2-empty">Aún no se detectan pagos mensuales.</div>'}</div><div class="b2-tool"><h3>Anomalías</h3><p>Importes significativamente superiores al histórico.</p>${an.length?`<div class="b2-list">${an.map(a=>`<div class="b2-row"><div><strong>${esc(a.merchant)}</strong><small>${esc(a.category)} · media ${money(a.average)}</small></div><span class="b2-pill b2-warn">${money(a.amount)}</span></div>`).join("")}`:'<div class="b2-empty">No se han encontrado anomalías relevantes.</div>'}</div></div></section><section class="b2-section"><h2>Inversiones</h2><p>Posiciones registradas.</p>${data.investments.length?`<div class="b2-list">${data.investments.slice(0,8).map(i=>`<div class="b2-row"><div><strong>${esc(i.name||i.ticker||"Inversión")}</strong><small>${esc(i.ticker||"")}</small></div><span class="b2-pill">${money(i.current_value||0)}</span></div>`).join("")}</div>`:'<div class="b2-empty">No hay inversiones registradas.</div>'}</section><section class="b2-section"><h2>Objetivos</h2><p>Progreso de tus metas.</p>${data.goals.length?`<div class="b2-list">${data.goals.map(g=>{const target=Number(g.target_amount||0),cur=Number(g.current_amount||0),r=target?cur/target:0;return`<div><div class="b2-row"><div><strong>${esc(g.name)}</strong><small>${money(cur)} de ${money(target)}</small></div><span class="b2-pill">${percent(r)}</span></div><div class="b2-bar"><i style="width:${Math.min(100,r*100)}%"></i></div></div>`}).join("")}</div>`:'<div class="b2-empty">No hay objetivos configurados.</div>'}</section></div></div><div class="b2-sub">BorjaAI 2.0 · Cifras calculadas desde el motor financiero compartido y los datos persistidos de tu cuenta.</div></div>`}
async function open(){injectStyle();let panel=document.getElementById(PANEL_ID);if(!panel){panel=document.createElement("div");panel.id=PANEL_ID;panel.className="b2-panel";document.body.appendChild(panel)}panel.innerHTML='<div class="b2-wrap"><div class="b2-top"><div><div class="b2-kicker">BorjaAI · Financial Intelligence</div><h1>Centro financiero 2.0</h1><p>Analizando tus datos…</p></div><button class="b2-close" data-b2-close>Cerrar</button></div><div class="b2-empty">Cargando análisis financiero…</div></div>';try{const data=await getData();panel.innerHTML=render(data);bind(panel,data)}catch(e){panel.innerHTML=`<div class="b2-wrap"><div class="b2-top"><div><div class="b2-kicker">BorjaAI · Financial Intelligence</div><h1>Centro financiero 2.0</h1></div><button class="b2-close" data-b2-close>Cerrar</button></div><div class="b2-empty">${esc(e.message||"No se pudo cargar el análisis.")}</div></div>`}}
function bind(panel,data){panel.querySelectorAll("[data-b2-close]").forEach(b=>b.addEventListener("click",()=>panel.remove()));const btn=panel.querySelector("[data-b2-afford]");if(btn)btn.addEventListener("click",()=>{const amount=Number(panel.querySelector("#b2-afford")?.value||0),result=panel.querySelector("#b2-afford-result");if(!amount){result.textContent="Introduce un importe válido.";return}const a=financialEngine.affordability(data,amount);if(a.after<0)result.innerHTML=`<span class="b2-bad">🔴 No te lo recomiendo ahora.</span><br>La compra dejaría la liquidez en ${money(a.after)}.`;else if(!a.affordable)result.innerHTML=`<span class="b2-warn">🟠 Podrías, pero con prudencia.</span><br>Después quedarían ${money(a.after)} y la reserva futura prevista es ${money(a.recurringReserve)}.`;else result.innerHTML=`<span class="b2-good">🟢 El impacto parece asumible.</span><br>Después quedarían ${money(a.after)}.`})}
function addNav(){if(document.getElementById(NAV_ID))return;const nav=document.querySelector(".side-nav");if(!nav)return;const b=document.createElement("button");b.type="button";b.id=NAV_ID;b.className="nav-link b2-nav";b.innerHTML='<span class="b2-nav-mark">2</span><span>BorjaAI 2.0</span>';b.addEventListener("click",open);nav.appendChild(b)}
function boot(){addNav();if(!document.getElementById(NAV_ID))setTimeout(boot,500);window.addEventListener("keydown",e=>{if(e.key==="Escape")document.getElementById(PANEL_ID)?.remove()});new MutationObserver(addNav).observe(document.body,{childList:true,subtree:true})}
boot();
